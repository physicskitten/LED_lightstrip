echo "# LED_lightstrip" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/physicskitten/LED_lightstrip.git
git push -u origin main

#include <FastLED.h>
#define LED_PIN    12
#define LED_TYPE    SK6812
#define NUM_LEDS    144
#define COLOR_ORDER GRB
#define NUM_COLOURS 2
#define CONSEC_LED 5
CRGB leds[NUM_LEDS];

#define BRIGHTNESS          96
#define FRAMES_PER_SECOND  120

void setup() {
  // put your setup code here, to run once:
  FastLED.addLeds<LED_TYPE, LED_PIN, GRB>(leds, NUM_LEDS);
  FastLED.setMaxPowerInVoltsAndMilliamps(5,500);
  FastLED.clear();
  FastLED.show();

}

void loop() {
  // put your main code here, to run repeatedly:
  // RED     GREEN    BLUE  
  //leds[0] = CRGB(255, 0, 0);
  //leds[1] = CRGB(0, 255, 0);
  //leds[2] = CRGB(0, 0, 255);
  //FastLED.show();

  //getting the red and blue alternating colours
  int i; //declare i again for some reason
  for (int i = 0; i < NUM_LEDS; i++) {
    if ((i/CONSEC_LED) % NUM_COLOURS == 0) {
      leds[i] = CRGB(255, 0, 0); //Red light
    } else {
      leds[i] = CRGB(0, 0, 255); //Blue light
    }
  }
  FastLED.show();
  delay(100);
}
